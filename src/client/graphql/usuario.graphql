#import "./fragments/user-values.graphql"

query ShowAllUsers {
  showUsers {
    ...UserValues
  }
}

mutation UpdateUser($input: UserUpdateInput!) {
  updateUser(input: $input) {
    ...UserValues
  }
}
